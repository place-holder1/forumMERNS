MONGO_URI=mongodb+srv://adminUser1:<l7pGTQSjsXGwucN2>@datacluster1.lctn3.mongodb.net/users?retryWrites=true&w=majority&appName=DataCluster1

# the app.get in server.js will change based on what's being called. so /users must be the same in .env and server.js for example